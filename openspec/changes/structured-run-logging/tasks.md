- [x] 新增 `internal/observability` 结构化日志工具。
- [x] 在进程启动时配置 slog JSON handler。
- [x] ChatService run 生命周期日志带 `run_id`、`thread_id`、`user_id`、`mode`。
- [x] run 完成日志输出 `status` 和 `duration_ms`。
- [x] RunEventWriter 和 run cancellation 错误日志切到结构化 run logger。
- [x] 补充结构化日志字段单元测试。

