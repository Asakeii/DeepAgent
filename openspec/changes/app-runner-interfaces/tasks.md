- [x] 新增 `ResearchRunner`、`CheckinRunner`、`ReminderStreamer` 接口。
- [x] 新增 `NewChatServiceWithDeps` 依赖注入构造函数。
- [x] 保持 `NewChatService` 默认真实 runner 行为不变。
- [x] 将 Checkin agent 调用移动到默认 runner 适配器。
- [x] 增加 ChatService fake runner 编排测试。

