# Tasks

- [x] 新增 server 配置：`allowed_origins`。
- [x] 新增 server 配置：`max_body_bytes`。
- [x] 将 HTTP middleware 从 `Access-Control-Allow-Origin: *` 改为白名单校验。
- [x] 增加统一请求体大小限制。
- [x] 补充配置解析和 HTTP middleware 单元测试。
- [ ] 后续：API auth / API key。
- [ ] 后续：rate limit。
- [ ] 后续：按路由配置 body limit。
